const loginForm = document.querySelector(".login_form");
const studentId = document.getElementById("login_student_id");
const password = document.getElementById("login_password");

loginForm.addEventListener("submit", (e) => {
  e.preventDefault();
});
